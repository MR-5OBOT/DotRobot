# black — pure black, the original from black.ini
# Background-only override. Included after the palette in kitty.conf,
# so it wins on every key it repeats and leaves the rest of the scheme alone.

background            #000000
color0                #000000
color8                #333333

active_border_color   #333333
inactive_border_color #121212

active_tab_background   #000000
inactive_tab_background #121212
tab_bar_background      #121212
