# oxocarbon — neutral, matches fsociety.ini
# Background-only override. Included after the palette in kitty.conf,
# so it wins on every key it repeats and leaves the rest of the scheme alone.

background            #161616
color0                #262626
color8                #525252

active_border_color   #525252
inactive_border_color #262626

active_tab_background   #161616
inactive_tab_background #262626
tab_bar_background      #0d0d0d
