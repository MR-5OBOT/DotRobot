-- Input Settings

hl.config({
    input = {
        kb_layout = "us",
        follow_mouse = 1,
        sensitivity = -0.05,
        natural_scroll = false,
        touchpad = {
            scroll_factor = 1.5
        }
    }
})
