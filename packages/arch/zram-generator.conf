# Half of usable RAM, capped at 8 GiB of logical swap capacity.
# Physical RAM use depends on stored data, compression, and metadata overhead.
[zram0]
zram-size = min(ram / 2, 8192)
# setup-zram.sh falls back to the kernel default if zstd is unavailable.
compression-algorithm = zstd
swap-priority = 100
