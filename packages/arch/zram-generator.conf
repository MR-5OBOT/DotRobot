# Compressed swap in RAM. Browser tabs and dev tools compress ~3x with zstd,
# so 8G of zram holds far more than 8G of pages. Takes no RAM while unused.
[zram0]
zram-size = min(ram, 8192)
compression-algorithm = zstd
