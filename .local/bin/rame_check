#!/usr/bin/env bash

while true; do
    # Get RAM usage
    ram_usage=$(free -h | grep Mem | awk '{print $3 "/" $2}')
    # Display notification
    notify-send "RAM Usage" "Current RAM Usage: $ram_usage"
    # Wait for 5 minutes (300 seconds)
    sleep 300
done
one
