# # Terminal settings (optimized for Kitty)
# set -g default-terminal "tmux-256color"
# set -ag terminal-overrides ",xterm-kitty:Tc,tmux-256color:Tc"
# # Undercurl support (Kitty supports this natively)
# set -ag terminal-overrides ',*:Smulx=\E[4::%p1%dm'
# set -ag terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'
#
# # neofetch stuff
# set -g allow-passthrough on
#
# # Unbind default prefix and set new one
# unbind C-b
# set -g prefix C-Space
# bind C-Space send-prefix  # For nested tmux sessions
#
# # General settings
# set -g base-index 1
# set -g detach-on-destroy off
# set -g escape-time 0
# set -g renumber-windows on
# set -g set-clipboard on
# set -g mouse on
#
# # Reload config
# bind R source-file ~/.config/tmux/tmux.conf \; display-message "Config reloaded"
#
# # Window navigation
# bind H previous-window
# bind L next-window
#
# # Pane navigation (compatible with vim-tmux-navigator)
# bind h select-pane -L
# bind l select-pane -R
# bind k select-pane -U
# bind j select-pane -D
# bind f resize-pane -Z
#
# # Pane resizing
# bind -r Left resize-pane -L 5
# bind -r Right resize-pane -R 5
# bind -r Up resize-pane -U 5
# bind -r Down resize-pane -D 5
#
# # Window splitting
# bind | split-window -h -c "#{pane_current_path}"
# bind - split-window -v -c "#{pane_current_path}"
#
# # Additional keybindings
# bind d detach-client
# # bind C-n command-prompt -p "New session name:" "new-session -s '%%'"
# bind C-j display-popup -E "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"
#
# # Set vi-mode
# setw -g mode-keys vi
#
# # Copy mode bindings
# setw -g mode-style 'fg=#DD4D8B bg=#000000 bold'
#
# # Panes border color
# set -g pane-active-border-style 'fg=#BE4277'
# set -g pane-border-style 'fg=#161616'
#
# # Copy mode
# setw -g mode-style 'fg=#DD4D8B bg=#000000 bold'
#
# # Status bar
# set -g status on
# set -g status-position top
# set -g status-justify centre
# set -g status-interval 1
# set -g status-style 'fg=colour1 bg=default'
# set -g status-left '#[fg=#BE4277,bold] [#S] '
# set -g status-right '#[fg=#BE4277]#{?client_prefix,#[reverse]<Prefix>#[noreverse] ,}#[fg=#8F70C0]%H:%M #[fg=#BE4277]%d-%b-%y '
# set -g status-left-length 50
# set -g status-right-length 50
#
# # Highlight the current window
# setw -g window-status-current-style 'fg=#8F70C0 underscore'
# setw -g window-status-current-format ' #I #W #F '
# setw -g window-status-style 'fg=colour1 dim'
# setw -g window-status-format ' #I #[fg=colour7]#W #[fg=colour1]#F '
#
# # Activity monitoring (disabled to remove highlights)
# setw -g monitor-activity off
# set -g visual-activity off
# set -g visual-bell off
#
# # Plugins
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'tmux-plugins/tmux-yank'
#
# # Initialize TPM
# run '~/.tmux/plugins/tpm/tpm'


# ┌──────────────────────────┐
# │                          │
# │     ░▀█▀░█▄█░█░█░█░█     │
# │     ░░█░░█░█░█░█░▄▀▄     │
# │     ░░▀░░▀░▀░▀▀▀░▀░▀     │
# │                          │
# └──────────────────────────┘
# Reload config
bind R source-file ~/.config/tmux/tmux.conf \; display-message "Config reloaded"

# Set default shell
set-option -g default-shell $SHELL

# Set true color
set-option -sa terminal-overrides ",xterm*:Tc"

# Set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Set mouse mode on
set -g mouse on

# Additional keybindings
bind d detach-client
bind C-n command-prompt -p "New session name:" "new-session -s '%%'"

# Window navigation
bind H previous-window
bind L next-window

# Pane navigation (compatible with vim-tmux-navigator)
bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D
bind f resize-pane -Z

# Pane resizing
bind -r Left resize-pane -L 5
bind -r Right resize-pane -R 5
bind -r Up resize-pane -U 5
bind -r Down resize-pane -D 5

# Start window numbering at 1
set -g base-index 1
setw -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Theme: borders
set -g pane-active-border-style 'fg=#BE4277'
set -g pane-border-style 'fg=#161616'

# Status bar configuration (moved to the bottom)
set -g status on
set -g status-position top           # Move the status bar to the bottom
set -g status-justify centre            # Center the status text
set -g status-interval 1                # Update status every second
set -g status-style bg=default,fg=brightblack  # Dark background, bright black text

# Status left and right formatting
set -g status-left "#[fg=brightblack,bright]Session: #[fg=brightblack]#S"
set -g status-right "#[fg=#BE4277]#{?client_prefix,#[reverse]<Prefix>#[noreverse] ,}#[fg=brightblack]#{window_name}"

# Theme: status (windows)
set -g window-status-format "#[fg=#B04D6E]●"
set -g window-status-current-format "#[fg=#BE4277]●"
set -g window-status-current-style "#{?window_zoomed_flag,fg=#D55FA3,fg=#FBB6D0}"
set -g window-status-bell-style "fg=#D55FA3,nobold"
set -g status-justify absolute-centre

# Set vi-mode
set-window-option -g mode-keys vi

# Copy mode
set -g set-clipboard on
setw -g mode-style 'fg=#DD4D8B bg=#000000 bold'

set -g allow-passthrough on

# Activity monitoring (disabled to remove highlights)
setw -g monitor-activity off
set -g visual-activity off
set -g visual-bell off

# # Window splitting
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Plugin manager
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

run '~/.tmux/plugins/tpm/tpm'
